{
  "name": "dt-forge-printer-link",
  "version": "0.1.0",
  "private": true,
  "description": "DT Forge printer-agent link — read-only Bambu X1C status/AMS + send-file, LAN mode. Unverified against real hardware; unit-tested against mocked MQTT/FTP.",
  "type": "module",
  "scripts": {
    "test": "node --test test/*.test.js"
  },
  "license": "UNLICENSED",
  "dependencies": {
    "basic-ftp": "^6.2.1",
    "mqtt": "^5.15.2"
  }
}
